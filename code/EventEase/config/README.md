# Configuration Files

This folder contains configuration files for various services and tools used in the project.

## Structure

- **firebase/** - Firebase configuration files
  - `firestore.rules` - Firestore security rules
  - `firestore.indexes.json` - Firestore database indexes configuration

Note: `firebase.json` and `.firebaserc` remain in the project root as they are required by Firebase CLI.

